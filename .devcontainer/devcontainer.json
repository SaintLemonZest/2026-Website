{
  "name": "MariaDB Development Environment",
  "onCreateCommand": "sudo apt-get update && sudo apt-get install -y mariadb-server mariadb-client",
  "postStartCommand": "sudo service mariadb start && mysql -e \"CREATE DATABASE IF NOT EXISTS dev_db; CREATE USER IF NOT EXISTS 'developer'@'localhost' IDENTIFIED BY 'password'; GRANT ALL PRIVILEGES ON *.* TO 'developer'@'localhost'; FLUSH PRIVILEGES;\"",
  "forwardPorts": [3306],
  "customizations": {
    "vscode": {
      "extensions": [
        "cweijan.vscode-mysql-client2"
      ]
    }
  }
}
